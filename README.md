# JavaScript Learning Journey 🚀

Documenting my path to mastering JavaScript from scratch — building toward the MERN stack (MongoDB, Express, React, Node.js).

## 📌 About

This repo tracks my day-by-day progress learning JavaScript, along with practice problems, mini-projects, and notes. Goal: build a strong JS foundation before moving into backend (Node/Express), databases (MongoDB), and frontend (React).

## 🗺️ Roadmap

```
JavaScript (in progress) → Node.js/Express → MongoDB → React → AI Integration
```

## ✅ Topics Covered

### Fundamentals
- [x] What is JavaScript & core fundamentals
- [x] Variables — `var`, `let`, `const`
- [x] Data types & type coercion
- [x] Numbers, `NaN`, `Math.floor()`
- [x] Operator precedence & assignment operators
- [x] Identifier rules & reserved words
- [x] Booleans, truthy/falsy values
- [x] String indices & concatenation
- [x] `null` vs `undefined`
- [x] TypeScript overview

### Control Flow & Loops
- [x] `for` loops, nested `for` loops
- [x] `while` loops, infinite loops
- [x] `break` keyword
- [x] Looping over arrays & nested arrays
- [x] `for...of` loops, nested `for...of`

### Data Structures
- [x] Object literals
- [x] Working with `this` keyword (call-site binding, implicit/explicit/default binding)
- [x] - [ ] Functions (regular & arrow)
- [x] Array methods (`map`, `filter`, `reduce`, `forEach`, etc.)
- [x] Destructuring & spread/rest operators

### Coming Up
- [ ] Scope & closures
- [ ] Callbacks, Promises, `async/await`
- [ ] DOM manipulation & events
- [ ] Fetch API / working with JSON
- [ ] ES6 modules

## 🛠️ Projects (Practice Applications)

Applying every new concept to real, ongoing projects instead of isolated exercises:

| Project | Description | Status |
|---|---|---|
| **Spotify Clone** | Music player UI (sidebar, song cards, player bar) with JS being added incrementally | In progress |

## 📂 Repo Structure

```
├── fundamentals/       # Basic syntax, variables, data types
├── loops/              # Loop exercises and patterns
├── objects/            # Object literal practice
├── projects/
│   ├── spotify-clone/
│   └── quora-clone/
└── notes/               # Concept notes and interview-style Q&A
```

## 📖 How I'm Learning

- One topic set per session, applied immediately to a real feature in one of the projects above
- Practicing DSA problems in parallel (tracked via LeetHub)
- Interview-style questions reviewed for every major concept (e.g. `this`, closures, hoisting)

## 🎯 Goal

Build strong enough JS fundamentals to move confidently into full-stack development (MERN) and eventually AI-integrated web applications, with product-company-level placement readiness as the target.

---
*Last updated: check commit history for latest progress*
